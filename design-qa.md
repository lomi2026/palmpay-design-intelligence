# v9-1 visual parity QA

**Comparison target**

- Source visual truth: `https://lomi2026.github.io/palmpay-design-intelligence/`
- Formal implementation: local Next.js routes in `apps/web`
- Required viewports: desktop and `390 x 844` mobile
- Required state: default dark theme, top of each route, then every visible interactive state

**Evidence status**

- The reference URL resolves to the expected PalmPay Design Intelligence Hub title.
- Captured the deployed public-home desktop default state at `1280 x 720` in the in-app browser on 2026-07-16, including its DOM structure and computed layout measurements.
- Captured the matching local public-home desktop default state at `1280 x 720`; the first comparison found P1 structural differences in the header and hero. The home header, hero copy, grid, controls and workspace-preview geometry were then corrected and locally re-captured.
- Captured the deployed `workspace.html` desktop default state and DOM on 2026-07-17. The live HTML checksum matches the local historical release snapshot from `origin/main` exactly (`5cf2ad16…c2da`), so the snapshot is valid evidence when the hosted browser page is slow to settle.
- Restored PostgreSQL, applied the pending Prisma migration, started the formal API, and authenticated the local development workspace with the restored `lomi2026@126.com` member account.
- Captured a same-state, side-by-side in-app-browser comparison of `http://localhost:4174/workspace.html` (the checksum-matched v9-1 source) and `http://localhost:3000/workspace` at `1280 x 720`. The source and local hero frame, card geometry, text wrapping, journey graphic, action sizes, metric start line, sidebar density and top-bar hierarchy were corrected in this iteration. The latest browser capture has no local console errors.
- Captured the archived Design Assets module by opening `设计资产` from the checksum-matched local workspace snapshot, then compared it side-by-side with `http://localhost:3000/workspace/design-assets` at `1280 x 720`. The local page is backed by 8 idempotently imported v9-1 assets, formal content versions, categories, tags and AssetDetail records—not placeholder data. The archival comparison informed the header, filter frame, 3-column grid, 160px asset covers, metadata, copy, search, platform filter, category filters and card/list toggles. It is not a deployed-page acceptance pair: direct verification now confirms the deployed `design-assets.html` URL is 404.
- Automated deployed/authenticated desktop captures for the public home, workspace and AI project library were restored on 2026-07-18. The comparisons identified a missing constrained desktop header container on the public home and a missing project-portfolio overview/suggested-priority layer; both are now corrected in the formal code using the established component system and real published project data. These are implementation findings, not final acceptance, until the new revision is deployed and captured at the same viewport.
- The mobile capture request at `390 x 844`, source captures for project-detail states and all interaction-state captures remain outstanding.

**Findings**

- [P1] Visual parity is incomplete outside the corrected public-home desktop hero.
  Location: public-home lower sections, the deployed workspace and project routes, and all required mobile states.
  Evidence: only the default desktop home state has a deployed-to-local capture pair; the deployed workspace and project counterparts still lack complete matched-state evidence.
  Impact: the requested 100% visual and interaction parity cannot be accepted.
  Fix: capture each matching state, compare it side by side, correct all P0/P1/P2 differences, and record each iteration here.

- [P1] The public-home desktop preview remains a structural approximation below the verified first viewport.
  Location: `/`, workspace preview interior and sections below the fold.
  Evidence: the outer frame, header, hero layout and visible controls were aligned from the captured source, but the remaining preview-content details and below-fold sections have not yet been compared against a source capture.
  Impact: it must not be represented as a complete or pixel-perfect home restoration.
  Fix: capture the full source page in incremental viewport screenshots and iterate against matching local captures.

- [P1] The workspace default desktop state is still not an exact state match because the restored local dump contains only member users.
  Location: `/workspace` sidebar and account menu.
  Evidence: the v9-1 source capture shows `Lomi Peng` and its management navigation; the formal local capture correctly shows the restored member `lomi2026`, omits reviewer/admin navigation by RBAC, and preserves the source's AI-project count badge.
  Impact: the formal implementation must not falsify identity or RBAC merely to imitate a static demo, but the required exact screenshot state cannot yet pass.
  Fix: obtain or define an approved local reviewer/admin test identity that corresponds to the v9-1 reference state, then recapture both permitted and member states.

- [P2] Workspace desktop residual geometry is within a few pixels but has not been accepted.
  Location: top-bar search/action alignment and the metric row offset.
  Evidence: source and local are now compared side by side at `1280 x 720`; the source account label has a different width, so the top-bar's auto-layout distributes a small remaining offset differently.
  Impact: this prevents a claim of 100% parity.
  Fix: repeat the capture with the approved matching user state, then tune the remaining header alignment from that exact content width.

- [P2] The archived Design Assets comparison is not valid deployed parity evidence.
  Location: `/workspace/design-assets` historical-reference record.
  Evidence: its local snapshot was useful for historical visual research, but direct verification on 2026-07-17 shows the corresponding deployed URL returns 404.
  Impact: no acceptance or blocker should be derived from that archived page; the formal route instead inherits the approved deployed workspace system.
  Fix: retain the formal favorite persistence and RBAC behavior as product requirements, but remove Design Assets from the deployed-counterpart screenshot gate.

**Open Questions**

- None. The approved reference is unambiguous; the blocker is capture reliability, not product intent.

**Implementation Checklist**

1. Complete the public-home desktop and mobile comparison, including all below-fold regions.
2. Capture each deployed route and key interaction state at matched desktop and mobile viewports.
3. Capture the matching authenticated/local formal route state for member and reviewer/admin permissions.
4. Compare the paired captures, fix every P0/P1/P2 mismatch, and record each iteration here.
5. Re-run the Design Assets comparison after formal favorites and the approved matching role state exist.

**Comparison history**

- 2026-07-17 — public-home desktop first viewport, deployed `https://lomi2026.github.io/palmpay-design-intelligence/`, implementation `http://localhost:3100/`, viewport `1280 x 720`, default dark state. A fresh same-viewport pair found the formal hero title start 4px low and the explanatory copy, CTA and metadata cluster about 10px high. The local hero eyebrow/title and CTA spacing were corrected from the measured source positions. Final measured source/local positions: title `y=311/310`, body `y=482/481`, primary CTA `y=574/573`, metadata `y=641/641`; the right workspace-preview heading remains aligned at `y=267`. No console errors were present. This accepts only the desktop first viewport evidence; below-fold and mobile states remain open.
- 2026-07-17 — workspace desktop, source `http://localhost:4174/workspace.html`, implementation `http://localhost:3000/workspace`, viewport `1280 x 720`, default dark state. The first authenticated local capture showed a 20px vertical offset, forced hero title line breaks, a too-large journey graphic and incorrect metric start. The formal workspace page and Shadcn-composed controls were adjusted; a fresh same-viewport side-by-side capture confirms those structural issues are corrected. Remaining P1/P2 findings are the account/role state mismatch and small content-width-dependent header offset above.
- 2026-07-17 — Design Assets desktop archival comparison, historical snapshot reached from `http://localhost:4174/workspace.html` via its `设计资产` navigation, implementation `http://localhost:3000/workspace/design-assets`, viewport `1280 x 720`, default dark state. The initial local page was an empty generic catalog. The 8 approved v9-1 assets were imported into PostgreSQL with source traceability; the formal page was rebuilt using Shadcn Input, Select, Button, Card and Badge components. The comparison informed asset covers, vertical card density, title/filter alignment, copy, categories, status labels and metadata. It is historical research only, not a deployed parity acceptance: the corresponding direct deployed URL was later confirmed as 404.
- 2026-07-17 — public home desktop continuation, source `https://lomi2026.github.io/palmpay-design-intelligence/`, implementation `http://localhost:3100/`, viewport `1280 x 720`, default dark state. The deployed source’s post-hero structure was recovered from matching release commit `bf39748` and implemented in the formal home: four metrics, three task paths, the 33-project portfolio, toolbar and four-card asset grid, verified AI practice, governance chain and CTA. The local full-page layout measures `4984px` high against the source’s `5195px`; structural order, source wording, desktop grid density and route targets are now present. This is not full acceptance: mobile, theme-toggle behavior and all remaining interaction-state evidence are still open.
- 2026-07-17 — public-home local mobile, implementation `http://localhost:3100/`, viewport `390 x 844`. The responsive source rules recovered from `bf39748` were applied: the workbench preview remains visible, the hero title reduces to `44px`, the desktop workspace CTA hides below `561px`, and an openable mobile menu exposes six navigation routes. The menu test confirmed `aria-expanded="true"` and six visible links. The deployed remote tab did not expose a viewport capability, so a like-for-like hosted mobile screenshot still remains open.
- 2026-07-17 — AI Project detail source/template inspection, deployed P01 template from `bf39748:assets/project-detail-p01-fbb774931e86.js`, formal route `/workspace/ai-projects/[slug]`. The formal page now follows the source hierarchy for the hero, exploration outcomes, management summary and project context, but binds those regions to persisted `AIProjectDetail`, `ContentVersion`, owner/team and priority values. A same-state screenshot cannot yet be captured without an approved authenticated browser identity, so this is a structural restoration rather than parity acceptance.
- 2026-07-17 — AI Case detail field inspection, formal route `/workspace/ai-cases/[slug]`. The formal page uses the approved home-page verified-practice language for before/after, responsibility, evidence and result hierarchy, binding every non-decorative value to `CaseDetail` or `ContentVersion`. Absent limits are clearly marked incomplete. There is no confirmed standalone deployed case-detail counterpart, so this remains a formal-only route rather than a parity-acceptance target.
- 2026-07-17 — Design Asset detail field inspection, formal route `/workspace/design-assets/[slug]`. The formal detail is a decision brief: applicability and platform scope appear before persisted usage guide, body, constraints, attachment count and content relations. The current deployment has no matching asset-detail artifact, so it inherits the workspace system and is not a separate screenshot-acceptance target.
- 2026-07-17 — AI Skill detail source/field inspection, formal route `/workspace/ai-skills/[slug]`. The checksum-verified `bf39748` release tree does not contain the historical standalone `skill-detail-*` artifact, and direct deployed checks confirm both `skill-detail-component-governance.html` and `ai-skill-toolkit.html` return GitHub Pages 404. No visual template was inferred from the older archival snapshot. The formal page instead implements Design Decision 12 using persisted `SkillDetail` fields: scope, input/output, Prompt, execution, examples, human review, limitations, version and owner. Missing values remain explicit. These are formal-only routes and should inherit the deployed workspace system rather than enter the counterpart parity gate.
- 2026-07-17 — direct deployed deep-link verification: `design-assets.html`, `upload-asset.html`, `design-intelligence-hub-demo.html`, `ai-skill-toolkit.html` and `skill-detail-component-governance.html` all return `Page not found · GitHub Pages`; `workspace.html` remains available as `工作台 · PalmPay Design Intelligence Hub`. The release-tree inspection agrees with this result. The parity inventory was corrected so that only actual deployed counterparts remain in the mandatory screenshot gate; the 404 routes are formal-only and inherit the approved workspace system.
- 2026-07-17 — direct deployed project verification: `projects/index.html` resolves to `工作台 · PalmPay Design Intelligence Hub` and `projects/project-detail.html` resolves to `AI 项目详情 · PalmPay Design Intelligence Hub`. These are retained as mandatory deployed counterparts; authenticated formal desktop/mobile comparison is still required.
- 2026-07-18 — deployed public-home desktop, source `https://lomi2026.github.io/palmpay-design-intelligence/`, formal test `https://palmpay-design-intelligence-web.vercel.app/`, default dark state. The source header content is constrained to the centered desktop content width while the formal header was edge-to-edge. The formal `V9Home` header container was corrected to the measured source-style `1248px` maximum content width with the mobile inset retained. A post-deployment matched screenshot is still required.
- 2026-07-18 — authenticated workspace and AI-project-library desktop, source `workspace.html#ai-projects`, formal test `/workspace/ai-projects`, default dark state. The workspace shell, navigation density, top bar and data-backed project table use the same source system, but the source library also has a portfolio summary and suggested-priority layer ahead of the table. The formal project library now restores that scan layer from persisted published records, keeps domain/value/stage filters, and does not create static mock status. Mobile and post-deployment comparisons remain open.
- 2026-07-18 — AI Project detail responsive source pass, source `bf39748:assets/project-detail-p26-46d79cc14a01.js`, formal route `/workspace/ai-projects/[slug]`. The deployed template's responsive rules were recovered directly: the hero uses a `620px` desktop minimum, `44px` mobile title, `44px 22px 32px` mobile copy padding, `58px/68px` desktop copy padding, and `22px` mobile / `34px 28px` desktop outcome-panel padding. The formal route now matches those source values while retaining persisted engagement and lifecycle controls. Automated remote screenshot capture timed out twice while acquiring this page, so this is a source-backed implementation correction, not screenshot acceptance.
- 2026-07-18 — Public-home interaction pass, source `bf39748:index.html`, formal route `/`. The formal theme control now reads and writes the source-compatible `ppux-theme` and legacy `pp-theme` preferences, applying the deployed light/dark source palette only to the public home. The source's mobile-only `assets`, `cases`, and `governance` collapse behavior is also restored at the same `720px` breakpoint: sections start collapsed on mobile, their controls expose the source wording and ARIA state, and an in-page anchor reveals its target section. This preserves formal route links and data-backed controls. It is not a screenshot acceptance: automatic source/formal capture remains unavailable after repeated browser timeouts.
- 2026-07-18 — Workspace theme interaction pass, source `bf39748:workspace.html`, formal `/workspace/*`. The workspace header no longer presents the source theme control as a disabled placeholder. It now uses the same persisted v9-1 preference and applies the scoped source light/dark palette to the entire authenticated workspace shell, without touching any formal RBAC, notification, lifecycle, or server-data behavior. Matching light-state capture remains required before acceptance.
- 2026-07-18 — AI Project detail navigation interaction pass, source `bf39748:assets/project-detail-nav-b576647563.js`, formal `/workspace/ai-projects/[slug]`. The formal project detail now restores the deployed fixed reading-progress indicator and mobile-safe `目录` drawer. Its outline derives only from the formal detail's persisted management, context, prioritization and traceability sections; lifecycle, favorite and engagement controls remain intact. Web typecheck, lint and API regression tests pass. A same-viewport source/formal interaction capture remains required before acceptance.
- 2026-07-18 — Workspace global-search interaction pass, source `bf39748:workspace.html`, formal workspace shell. The header search control and `⌘/Ctrl + K` shortcut now open an accessible source-style search dialog rather than immediately navigating away. Submission carries the query into the existing formal, permission-filtered search route; it does not create client-side search results or bypass server authorization. Web typecheck and lint pass. A matched source/formal dialog-state capture remains required before acceptance.
- 2026-07-18 — AI Skill contribution and copy interaction pass, source `legacy/v9-1/ai-skill-toolkit.html` and its matching historical `skill-detail-*` templates, formal `/workspace/ai-skills`, `/workspace/ai-skills/[slug]`, and `/workspace/submit?type=AI_SKILL`. The formal catalog now exposes `新增 Skill` only to users holding `content.create`; it opens the existing server-backed draft workflow with `AI_SKILL` selected, rather than a static modal. The source's prompt-copy interaction is restored beside the persisted Prompt template with feedback, while content creation, draft saving, review and publishing remain enforced by the formal API/RBAC path. The corresponding `新增资产` query parameter is now also honored. Web typecheck, lint and diff validation pass. This is a source-backed interaction correction; the archived source routes are 404 in the currently deployed legacy site and are therefore not screenshot-acceptance counterparts.
- 2026-07-18 — Public-home project deep-link interaction pass, source `bf39748:index.html` and deployed `projects/project-detail.html`, formal `/workspace/ai-projects/[slug]`. The four source portfolio cards and the 90-day route now open their matching imported, persisted project records (`S05`, `P01`, `P08`, `P23`) rather than stopping at the library. The formal project page honors the source navigation origin with a return link to the public project portfolio; normal catalog access still returns to the data-backed project library. Web typecheck, lint and diff validation pass. A same-viewport authenticated screenshot remains required before visual acceptance.

**Follow-up Polish**

- None until the blocking source evidence is available.

final result: blocked
