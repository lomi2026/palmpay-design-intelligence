# 分类与标签联动 — 本地验收记录

日期：2026-09-04。范围：用户确认的分类/标签选择、停用规则、版本发布同步、真实关联数量与查看入口。不包含批量迁移或标签合并。

## 行为与 API

- `GET /api/content-drafts/taxonomy`：登录且具有 `content.read` 权限的用户，读取当前组织启用的分类/标签。分类包含适用内容类型。
- `POST /api/content-drafts`、`PATCH /api/content-drafts/:id`：接受 `categoryId` 与 `tagIds`（最多 50 个、不重复的 UUID）。PATCH 省略字段表示保留；`categoryId: null` / `tagIds: []` 表示清除。
- 创建和保存会校验组织、启用状态和分类适用类型。只有该草稿原有的停用关联可以保留。直接伪造 `body.taxonomy` 不能绕过验证。
- `GET /api/content-drafts/:id`：继续校验作者或获授权编辑者身份，增加 `taxonomy` 和 `taxonomyOptions`，后者包含已选择的停用项，供编辑界面标记历史关联。
- 正式目录返回分类/标签状态；普通筛选仅提供启用项，历史内容与其标签展示保留。显式历史筛选链接保持可用，权限规则不变。
- 管理分类/标签的 `usageCount` 改为实际当前关联数；`GET /api/admin/contents?categoryId=...` / `?tagId=...` 支持受 `content.edit_all` 保护的分页查看。排除软删除内容，包含未发布内容的当前关联，不包含历史版本或已发布内容尚未发布的修改。
- 不增加数据库实体或迁移；通过现有版本 JSON 快照与关系表完成。停用只控制新增选择，不回写或删除历史版本。

## 自动化验证

- API 55/55 测试通过，0 跳过；包含 15 项真实 PostgreSQL 集成测试。
- 新增用例覆盖状态切换、默认停用、类型不匹配、组织校验、伪造嵌套快照、历史关联保留/移除、重新启用、实际关联数量、普通成员不能访问管理关联列表、两次审核发布与旧版本不可变。
- 既有四内容类型发布回归通过，Web 40/40 测试通过。
- Web / API 生产构建、类型检查通过。构建沙箱限制曾阻止 Turbopack 本地通信端口，允许本地构建后成功。

## 浏览器实测

使用本机生产构建、Chrome 与独立本地 PostgreSQL 验收副本：

1. 管理中心“可复制”从停用改为启用，保存后出现“保存成功”，选中值保持启用。
2. 新建设计资产：分类选项只显示适用的资产分类，启用后的“可复制”可选择。
3. 创建临时草稿，分类与标签正确回显；修改分类为“研究与策略”并保存，刷新后仍保持，预览显示相同内容。
4. “可复制”从原来的关联 1 项变为 2 项，点击后准确列出原发布内容和新草稿，并区分状态。
5. 标签恢复停用后，草稿与预览显示“可复制（已停用）”；设计资产普通筛选只剩“全部标签”。
6. 验收结束后，该临时草稿已软删除（可恢复），测试标签保持原停用状态；没有修改线上数据。

## 验收环境边界

- Web：`http://localhost:3000`；API：`http://127.0.0.1:3001`。
- 数据库：`palmpay_taxonomy_acceptance_20260904`，由原本地库复制；原本地库与远程 Render 数据均未被本轮测试改写。
- 本地副本执行了仓库已有的默认停用标签迁移。此处标签状态不是线上当前状态。
- 本次代码尚未推送、未部署线上；此前 GitHub 推送审批仍未解决。远程部署后需要再次验证前后端同版本及用户工作流。
